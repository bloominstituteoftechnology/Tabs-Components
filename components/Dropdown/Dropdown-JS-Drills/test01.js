// add dropdown functionality that toggles the dropdown hidden

class Dropdown {
	constructor {
		
	}
}

let dropdowns = document.querySelectorAll('.dropdown');
console.log('01_create nodelist from dropdown' + dropdowns);
dropdowns = Array.from(dropdowns).map();